for sam
Change filenames of music to just the # (save the names somewhere so you have them in case)
Change the filenames of the cries so that its just 1, 2, 3, etc instead of 001_pokemonname
make sure everything is content and copy if newer