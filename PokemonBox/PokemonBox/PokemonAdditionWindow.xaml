<Window x:Class="PokemonBox.PokemonAdditionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PokemonBox.Models"
        mc:Ignorable="d"
    xmlns:gif="http://wpfanimatedgif.codeplex.com"


        Title="PokemonAdditionWindow" Height="600" Width="500">
    <StackPanel x:Name="spPokemonCreator">
        <ComboBox x:Name="cmbSpeciesPicker" DisplayMemberPath="Species" SelectionChanged="cmbSpeciesPicker_SelectionChanged"/>
        <StackPanel x:Name="spSelectedSpecies">
            <TextBlock Text="{Binding Species}"/>
            <TextBlock Text="{Binding AnimatedSpritePath}"/>
            <Image gif:ImageBehavior.AnimatedSource="{Binding AnimatedSpritePath}"/>
        </StackPanel>
    </StackPanel>
</Window>
